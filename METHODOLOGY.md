# 方法论：为什么是这样设计

> 给人读，不进上下文。这里记录所有设计取舍和它们的理由。

## 一、问题定义

使用编程 Agent 时，模型选择是一个**每任务都要做、但几乎没人认真做**的决策。

两个方向的浪费同时存在：

- **大材小用**：格式化、改变量名、查 API 这类任务跑在旗舰模型上。用户为用不上的能力付钱。
- **小马拉车**：架构设计、跨系统排查交给经济模型。它不会报错，只会给你一个**看起来合理但错的方案** —— 这是更贵的浪费，因为你要花几倍时间去返工和排查。

第二种尤其隐蔽：**模型能力不足不会表现为失败，而表现为自信的平庸。**

## 二、与已有工作的边界

这个领域已经有很多工作，但它们解决的问题和我们不同：

| 工作 | 解决什么 | 为什么不适用 |
| --- | --- | --- |
| **RouteLLM**（Ong et al. 2024） | 训练路由器，自动把 query 分给强/弱模型 | 需要偏好数据和多模型 API 接入 |
| **FrugalGPT**（Chen et al. 2023） | 级联：先便宜的跑，不合格再升级 | 同上，且需要可靠的答案质量打分器 |
| **AutoMix**（Madaan et al. 2024） | 模型自评 + POMDP 决定升级 | 同上 |
| **claude-code-token-save**、**compass** 等 | 在 Agent 框架内自动切模型 | 硬编码单一厂商，且依赖工具暴露 model 参数 |

它们的共同前提：**系统能自己换模型**。

而我们面对的场景是：**人在 GUI 或 IDE 里手动选模型，系统换不了。**
此时唯一能做的是什么都不做地浪费，或者——**提醒人**。

这就是本项目的全部定位：**一个不会自己动手、只负责提醒的副驾驶。**

代价是效果依赖用户愿不愿意听；收益是零依赖、零改造、任何工具都能用。

## 三、设计取舍

### 为什么是三档，不是五档

五档的区分度在理论上更好，但**人（和模型）无法稳定地区分五档**。

三档是判断可靠性的上限。超过三档，打分本身就会消耗比省下更多的认知资源和 token。

档位设计的第一原则是**可判断性**，不是**精确性**。

### 为什么是这四个维度

选维度的唯一标准：**只看任务描述就能判断，不需要额外探索。**

被淘汰的候选维度：

- "需要读多少代码" —— 要读了才知道，判断成本高于收益
- "预计消耗多少 token" —— 事后才知道
- "是否需要联网" —— 与难度无关

留下的四个维度（推理深度、影响范围、出错代价、需求明确度）都能在 10 秒内从任务描述直接读出。

**特别说明「出错代价」**：它是唯一一个**不描述任务难度、而描述任务后果**的维度。
引入它的理由是：难度相同的两个任务，一个错了能一键重来，一个错了会导致数据丢失 —— 它们需要的模型档位不应该相同。

### 为什么不自动切换

技术上可行（有现成项目在这么做），但我们主动放弃：

1. 自动切换需要工具暴露 model 参数，牺牲通用性
2. 自动切换出错时用户不知道发生了什么
3. **最重要的**：本项目的目标是让人建立判断力，而不是替人做决定

工具应该是教练，不是代驾。

### 为什么判错不纠偏

如果 Agent 提示完又自我怀疑、回头改口，会产生两类成本：

- 额外的 token 消耗
- 用户对提示的信任度下降（"你自己都不确定，凭什么提醒我"）

**一次判断、一次提示、不再回头。** 判错的成本远低于反复纠结的成本。

## 四、token 账本：这东西真的划算吗

一份宣称帮你省 token 的规则，如果自己吃掉更多 token 就是讽刺。实算如下。

### 成本侧

L0 常驻规则：**87 token**（英文版）/ **118 token**（中文版）。

它每轮对话都会被计入输入。假设一个工作日 20 轮：

```
87 token × 20 轮 = 1,740 token/天
1,740 × $7.22/M ≈ $0.0126/天  ≈ $0.38/月（按 30 天）
```

注：Claude Code 等工具会自动 prompt caching，system prompt 部分命中缓存后读取成本约为原价的 1/10，实际数字更低。**这里按不缓存的最坏情况算。**

### 收益侧

保守假设：每天 20 个任务中，30%（6 个）是「轻任务跑在旗舰上」，每个平均 15K token。

```
6 × 15,000 × ($7.22 − $0.06)/M ≈ $0.645/天 ≈ $19.4/月
```

### 净结果

| 情形 | 月节省 | 月成本 | 倍数 |
| --- | --- | --- | --- |
| 30% 任务错配 | ~$19.4 | ~$0.38 | **~51×** |
| 10% 任务错配 | ~$6.5 | ~$0.38 | **~17×** |
| 5% 任务错配（极保守） | ~$3.2 | ~$0.38 | **~8×** |

**即使只有 5% 的任务存在错配，收益仍是成本的 8 倍。**

### 没算进去的收益

以下更难量化，但可能比上面更大：

- **避免返工**：小模型在重任务上给出"看起来对"的错误方案，排查成本通常是任务本身的数倍
- **避免限流打断**：订阅制下，把轻任务从中档降到轻档可以显著延长速率窗口

### 诚实的反面

如果你的任务分布本来就合理（轻任务用轻模型、重任务用旗舰），这份规则的收益接近零，而成本稳定存在。

**它只对「从不刻意选模型」的人有价值。** 如果你已经是精打细算的用户，不需要它。

## 五、中文 prompt 的真实成本：现象、机制、决策含义

实测（cl100k 分词器）：

| 版本 | token | 字符 | token/字符 |
| --- | --- | --- | --- |
| 英文 | 87 | 640 | 0.14 |
| 中文 | 118 | 224 | 0.53 |

中文的字符效率是英文的近 4 倍（224 字符 vs 640 字符表达同样内容），
但 **token 效率只有英文的约 1/4** —— 中文几乎一字一 token。

### 机制：跟模型参数量无关，是分词器的训练语料分布

一个常见误解是"因为模型参数是在英文上训的"。精确归因要分三层：

| 层面 | 是否相关 | 说明 |
| --- | --- | --- |
| 模型参数量（175B / 1T 之类） | ❌ 无关 | 参数量决定能力，不决定切分 |
| **分词器 vocab 的构建语料分布** | ✅ **决定因素** | BPE 在训练语料上学到的 merge 规则 |
| 分词器 vocab size | ⚠️ 只限容量 | 决定"能收多少"，不决定"收了什么" |

BPE 分词器的 vocab 名额是按语料中的**出现频率**分配的。语料里英文占绝对多数，
于是名额被英文单词和常见词缀占满，中文分到的很少。结果就是：

- **高频汉字**：占到一个名额，1 字 = 1 token
- **中频汉字/词**：不在 vocab，被拆成 2–3 个**字节级** token，1 字 = 2–3 token
- **英文整词**：`architecture`、`optimization` 直接命中 vocab，1 词 = 1 token

实测证据（这条最能说明问题）：

```
架构          -> 3 token    ["", "", "构"]    ← "架" 不在 vocab，退回 UTF-8 字节
architecture  -> 1 token    ["architecture"]    ← 英文整词命中 vocab
优            -> 1 token    ["优"]              ← 常用字在 vocab
```

注意"架"被拆成了字节级 token —— **这比"一字一 token"还糟，是一字三 token。**

压缩率实测：

| | 字符 | token | 字符/token |
| --- | --- | --- | --- |
| 中文 | 21 字 | 24 | 0.88 |
| 英文 | 67 字符 | 14 | 4.79 |

**单 token 承载的信息量，英文是中文的约 5.5 倍。**

> 这不是本项目的原创发现。BPE 语料分布导致非英语种 token 膨胀，是分词器领域的已知结论。
> 本项目做的只是把它接到一个具体的决策问题上：**模型选型**。

### 决策含义一：常驻规则应当用英文，哪怕你全程说中文

这条最反直觉，但逻辑很直接：

- **常驻规则是给机器读的，不是给人读的。** 可读性只对你写它那一次重要，
  而它每一轮都要进上下文 —— 成本是**按轮数累积**的，收益是一次性的。
- 输出语言仍然**完全跟随用户**。规则用英文，不代表回答要用英文。

所以本项目默认推荐英文版 L0（87 token），中文版（118 token）作为可读性选项保留。

**金额上的差异可以忽略**（每月一毛钱级别）：

```
(118 − 87) × 20 轮 × $7.22/M ≈ $0.0045/天 ≈ $0.13/月
```

真正的差异在**上下文预算**：长会话、规则文件多、或上下文窗口本就紧张时，
那 31 个 token 是有成本的；其他情况下，为可读性付这个价完全值得。

### 决策含义二：中文用户选型时要算两层账（推论，未实测）

推论：中文语料占比高的分词器（Qwen / GLM / DeepSeek / Kimi 等国产模型），
中文字词覆盖应更全，**同一份中文 prompt 的 token 数可能比欧美模型少 20–40%**。

⚠️ **这是从机制推出的推论，本项目没有实测验证**，列出来是因为它对选型有直接影响：

如果成立，中文用户选择国产模型是**双重优势叠加**：

```
总成本 = 单价 × token 数
         ↑         ↑
      本来就低    还可能更少
   ($0.06–2.02/M   (推论：−20~40%)
    vs $7.22–14.44/M)
```

想自己验证的话，三行就够（需要 `tiktoken` 和对应模型的 tokenizer，
国产模型一般可在 HuggingFace 或 ModelScope 取到）：

```python
enc = tiktoken.get_encoding("cl100k_base")   # 欧美系基线
# qwen_tok = AutoTokenizer.from_pretrained("Qwen/Qwen3-8B")
text = "你的中文 prompt"
print(len(enc.encode(text)))        # 对比
# print(len(qwen_tok.encode(text)))
```

**在验证之前，不要把这个数字写进选型表。** 一份宣称帮你省 token 的工具，
自己不能传播未经验证的数字。

### 决策含义三：成本估算不能照搬英文经验

英文社区流传的"这个任务大概 10K token"这类经验值，在中文语境下要打折理解 ——
同样的语义内容，中文任务的实际 token 数通常更高。做预算时留出余量。

## 六、失效模式

诚实地列出这个项目什么时候不管用：

1. **用户没法换模型** —— 公司统一配置、IDE 锁定、或买了单一厂商套餐。
   缓解：L0 允许给"同模型内替代方案"（调推理强度、拆小任务）。
2. **任务边界模糊** —— 一个长会话里混着轻中重各种子任务，单次判断失去意义。
   缓解：按子任务判断，而非按整个会话。
3. **模型格局剧变** —— 新模型发布会让 `rules/models.md` 瞬间过时。
   缓解：档位定义与模型名单分离，前者永不过期；后者标了 `valid_until`。
4. **提示疲劳** —— 如果用户从不换模型，反复提示会变成噪音。
   缓解：每会话最多一次；用户说"别提了"即永久关闭。

## 七、维护

| 文件 | 维护频率 | 触发条件 |
| --- | --- | --- |
| `rules/models.md` | 每季度 | 主要厂商发新模型 / 调价 |
| `rules/tier-table.md` | 半年 | 发现某类任务判断不准 |
| L0 规则 | 几乎不变 | 三档模型本身失效 |
| `guides/subscription-choice.md` | 半年 | 套餐形态变化 |

**最危险的错误是凭记忆推荐模型。** 模型名是本项目唯一会腐烂的部分，宁可只报档位。
